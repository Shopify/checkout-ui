import React from 'react';

import {Portal} from './Portal';

const meta = {
  component: Portal,
  title: 'Portal',
};

export default meta;

export const defaultState = () => <Portal>Hello</Portal>;
