import React from 'react';

export interface Props {
  /**
   * A unique identifier for the toggle button.
   */
  id: string;
  /**
   * The elements that are passed into the toggle button group
   */
  children?: React.ReactNode;
}
export function ToggleButton() {
  return <></>;
}
