import React from 'react';

import {SkeletonThumbnail} from './SkeletonThumbnail';

const meta = {
  component: SkeletonThumbnail,
  title: 'checkout-web-ui/SkeletonThumbnail',
};

export default meta;

export const defaultState = () => <SkeletonThumbnail />;
